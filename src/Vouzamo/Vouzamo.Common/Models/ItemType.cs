namespace Vouzamo.Common
{
    public enum ItemType
    {
        Repository,
        Folder,
        Contract,
        Component,
        Page
    }

    public enum FieldType
    {
        Text,
        Boolean
    }
}
